// FuelEfficiency.java - Interface
// An interface defines a contract: any class that implements it
// must provide an implementation for all its methods.

public interface FuelEfficiency {

    // Any class implementing this interface must define how mileage is calculated
    void calculateMileage();
}
